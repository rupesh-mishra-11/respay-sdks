<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow all certificates for development (remove in production) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
            <!-- Trust user-added certificates (for development) -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <!-- Specific domain configuration if needed -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">us-residentpay-bff.d05d0001.entratadev.com</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
